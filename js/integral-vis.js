// Integral Visualization
const integralVis = (() => {
    const regl = createREGL(document.getElementById('integral-vis'));
    let state = {
        numRectangles: 10,
        showArea: false
    };
    // ... rest of the code ...
})();