// Limit Visualization
const limitVis = (() => {
    const regl = createREGL(document.getElementById('limits-vis'));
    let state = { time: 0, animating: false };
    // ... rest of the code ...
})();